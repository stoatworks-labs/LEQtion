{
  "$schema": "../gen/schemas/desktop-schema.json",
  "identifier": "default",
  "description": "What the main window is allowed to do. Deliberately small: LEQtion reads one audio input and writes one settings file, both through its own commands, so it needs almost nothing from the plugin surface.",
  "windows": ["main"],
  "permissions": [
    "core:default",
    "opener:allow-open-url"
  ]
}
